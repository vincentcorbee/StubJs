const names = {
  content_type: 'content-type',
  x_mapped_to: 'x-mapped-to',
  x_proxied_to: 'x-proxied-to',
}

const values = {
  text_plain: 'text/plain',
  application_json: 'application/json',
}

module.exports = {
  names,
  values,
}
